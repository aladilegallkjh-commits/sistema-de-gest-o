import { useAuth } from "@/_core/hooks/useAuth";

import { trpc } from "@/lib/trpc";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { BarChart3, Bell, Boxes, BriefcaseBusiness, CheckCircle2, ChevronRight, CircleDollarSign, ClipboardCheck, Clock3, Factory, FileText, LayoutDashboard, LogIn, Menu, PackageCheck, Plus, Search, Settings2, ShieldCheck, ShoppingCart, Sparkles, TrendingUp, Truck, UserRound, UsersRound, X } from "lucide-react";
import { useMemo, useState } from "react";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

const modules = [
  { id: "dashboard", label: "Visão geral", icon: LayoutDashboard },
  { id: "commercial", label: "Comercial", icon: BriefcaseBusiness },
  { id: "production", label: "Produção", icon: Factory },
  { id: "stock", label: "Estoque & compras", icon: Boxes },
  { id: "suppliers", label: "Fornecedores", icon: Truck },
  { id: "costs", label: "Custos & margem", icon: CircleDollarSign },
  { id: "post-sale", label: "Pós-venda", icon: UserRound },
  { id: "reports", label: "Relatórios", icon: BarChart3 },
];

const demoProjects = [
  { code: "PRJ-024", name: "Modernização da recepção", client: "Grupo Orion", status: "Em produção", progress: 68, value: "R$ 18.500", margin: "44,7%", tone: "blue" },
  { code: "PRJ-021", name: "Identidade visual completa", client: "Casa Nômade", status: "Em aprovação", progress: 38, value: "R$ 12.800", margin: "38,2%", tone: "violet" },
  { code: "PRJ-019", name: "Sinalização corporativa", client: "Norte & Sul", status: "Pós-venda", progress: 100, value: "R$ 8.950", margin: "51,4%", tone: "emerald" },
];

const weekly = [{ day: "Seg", value: 4 }, { day: "Ter", value: 7 }, { day: "Qua", value: 5 }, { day: "Qui", value: 9 }, { day: "Sex", value: 8 }, { day: "Sáb", value: 3 }];
const stageData = [{ name: "Produção", value: 42, color: "#71717a" }, { name: "Aprovação", value: 23, color: "#8b5cf6" }, { name: "Entrega", value: 18, color: "#10b981" }, { name: "Pós-venda", value: 17, color: "#f59e0b" }];

function money(value: number) { return value.toLocaleString("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 }); }

export default function Home() {
  const { user, isAuthenticated } = useAuth();
  const [active, setActive] = useState("dashboard");
  const role = user?.role ?? "gestor";
  const allowedByRole: Record<string, string[]> = { admin: modules.map(item => item.id), gestor: modules.map(item => item.id), comercial: ["dashboard", "commercial", "reports"], producao: ["dashboard", "production", "stock"], compras: ["dashboard", "stock", "suppliers", "costs"], pos_venda: ["dashboard", "post-sale", "reports"], user: ["dashboard"] };
  const visibleModules = modules.filter(item => (allowedByRole[role] ?? allowedByRole.user).includes(item.id));
  const [mobileOpen, setMobileOpen] = useState(false);
  const summaryQuery = trpc.dashboard.summary.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const reportsQuery = trpc.reports.overview.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const projectsQuery = trpc.projects.list.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const productionQuery = trpc.production.list.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const clientsQuery = trpc.clients.list.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const summary = summaryQuery.data ?? { projects: 12, proposals: 8, orders: 16, clients: 34, stockAlerts: 3, overdueOrders: 2, budgetAlerts: 1, postSaleToday: 4 };
  const reports = reportsQuery.data ?? { sold: 18500, planned: 10200, actual: 9840, margin: 44.7, suppliersAtRisk: 1 };
  const currentModule = visibleModules.find(item => item.id === active) ?? visibleModules[0];
  const greeting = user?.name?.split(" ")[0] ?? "Leandro";
  const visibleProjects = useMemo(() => projectsQuery.data?.slice(0, 3).map((project, index) => ({ code: project.code, name: project.name, client: `Cliente #${project.clientId}`, status: project.status === "in_progress" ? "Em produção" : project.status === "post_sale" ? "Pós-venda" : "Planejamento", progress: project.status === "delivered" ? 100 : project.status === "in_progress" ? 68 : 38, value: money(Number(project.soldValue ?? 0)), margin: Number(project.soldValue ?? 0) ? `${(((Number(project.soldValue ?? 0) - Number(project.actualCost ?? 0)) / Number(project.soldValue ?? 0)) * 100).toFixed(1).replace(".", ",")}%` : "0%", tone: ["blue", "violet", "emerald"][index % 3] })) ?? demoProjects, [projectsQuery.data]);

  return <div className="min-h-screen bg-[#f5f7fb] text-[#172033]">
    <aside className={`fixed inset-y-0 left-0 z-50 w-[264px] border-r border-[#e5e9f2] bg-[#101828] text-white transition-transform duration-200 lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}>
      <div className="flex h-20 items-center justify-between border-b border-white/10 px-6">
        <div className="flex items-center gap-3"><img src="/logo.png" alt="Multiply Engineering Logo" className="h-9 w-9 object-contain rounded-lg" /><div><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-zinc-300">Multiply</p><p className="text-sm font-semibold tracking-tight text-white">Engineering</p></div></div>
        <button onClick={() => setMobileOpen(false)} className="lg:hidden"><X className="h-5 w-5 text-white/60" /></button>
      </div>
      <div className="px-4 pt-7"><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.18em] text-white/35">Operação</p><nav className="space-y-1">{visibleModules.filter(item => item.id !== "reports").map(item => { const Icon = item.icon; const selected = active === item.id; return <button key={item.id} onClick={() => { setActive(item.id); setMobileOpen(false); }} className={`group flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm transition ${selected ? "bg-white text-[#172033] shadow-xl shadow-black/10" : "text-white/60 hover:bg-white/10 hover:text-white"}`}><Icon className={`h-[17px] w-[17px] ${selected ? "text-zinc-800" : "text-white/45 group-hover:text-white"}`} /><span className="flex-1">{item.label}</span>{item.id === "dashboard" && <span className="rounded-full bg-rose-400 px-1.5 py-0.5 text-[10px] font-bold text-white">6</span>}</button> })}</nav><p className="mb-3 mt-8 px-3 text-[10px] font-bold uppercase tracking-[0.18em] text-white/35">Gestão</p><nav className="space-y-1">{visibleModules.filter(item => item.id === "reports").map(item => { const Icon = item.icon; return <button key={item.id} onClick={() => { setActive(item.id); setMobileOpen(false); }} className={`group flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm transition ${active === item.id ? "bg-white text-[#172033]" : "text-white/60 hover:bg-white/10 hover:text-white"}`}><Icon className="h-[17px] w-[17px] text-white/45" /><span>{item.label}</span></button> })}</nav></div>
      <div className="absolute bottom-0 w-full border-t border-white/10 p-4"><div className="flex items-center gap-3 rounded-xl bg-white/5 p-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-zinc-800 text-sm font-bold">{greeting.slice(0, 1)}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold">{user?.name ?? "Conta de demonstração"}</p><p className="truncate text-[11px] text-white/40">{user?.email ?? "gestor@empresa.com"}</p></div><Settings2 className="h-4 w-4 text-white/35" /></div></div>
    </aside>
    {mobileOpen && <button aria-label="Fechar menu" onClick={() => setMobileOpen(false)} className="fixed inset-0 z-40 bg-[#101828]/50 backdrop-blur-sm lg:hidden" />}
    <main className="min-h-screen lg:pl-[264px]">
      <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-[#e5e9f2] bg-[#f5f7fb]/90 px-5 backdrop-blur-xl sm:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="rounded-lg border border-[#e5e9f2] bg-white p-2 lg:hidden"><Menu className="h-5 w-5" /></button><div><p className="text-xs font-medium text-[#8792a8]">Quarta-feira, 19 de agosto de 2026</p><h1 className="mt-0.5 text-xl font-semibold tracking-tight">{currentModule.label}</h1></div></div><div className="flex items-center gap-2 sm:gap-4"><div className="hidden items-center gap-2 rounded-xl border border-[#e5e9f2] bg-white px-3 py-2 text-sm text-[#9aa3b5] md:flex"><Search className="h-4 w-4" /> Pesquisar <span className="ml-6 rounded bg-[#f0f2f7] px-1.5 py-0.5 text-[10px]">⌘ K</span></div><button className="relative rounded-xl border border-[#e5e9f2] bg-white p-2.5 text-[#64708a] hover:text-zinc-800"><Bell className="h-[18px] w-[18px]" /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-rose-500 ring-2 ring-white" /></button>{!isAuthenticated && <Button onClick={() => window.location.href = "/login"} className="hidden gap-2 rounded-xl bg-zinc-800 hover:bg-zinc-900 px-4 shadow-lg shadow-zinc-200 sm:flex"><LogIn className="h-4 w-4" /> Entrar</Button>}</div></header>
      <div className="mx-auto max-w-[1440px] px-5 py-7 sm:px-8 sm:py-9"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-sm font-medium text-zinc-700"><span className="h-2 w-2 rounded-full bg-emerald-500" /> Operação saudável</p><h2 className="text-3xl font-semibold tracking-[-0.04em] sm:text-[34px]">Bom dia, {greeting}.</h2><p className="mt-2 max-w-2xl text-sm text-[#7b879c]">Aqui está o panorama do seu negócio. Existem <strong className="font-semibold text-[#172033]">6 pontos</strong> que merecem atenção hoje.</p></div><Button onClick={() => setActive("commercial")} className="w-fit gap-2 rounded-xl bg-[#172033] px-4 shadow-xl shadow-slate-300/30"><Plus className="h-4 w-4" /> Nova proposta</Button></div>
        {active === "dashboard" ? <DashboardContent summary={summary} reports={reports} projects={visibleProjects} setActive={setActive} /> : <ModulePlaceholder active={active} onBack={() => setActive("dashboard")} />}
      </div>
    </main>
  </div>;
}

function DashboardContent({ summary, reports, projects, setActive }: { summary: any; reports: any; projects: typeof demoProjects; setActive: (id: string) => void }) {
  const metrics = [{ label: "Projetos ativos", value: summary.projects || 12, detail: "+12,4% vs. mês anterior", icon: BriefcaseBusiness, color: "zinc" }, { label: "Ordens em produção", value: summary.orders || 16, detail: "2 exigem atenção", icon: Factory, color: "violet" }, { label: "Valor em carteira", value: money(reports.sold || 18500), detail: "+8,1% no período", icon: CircleDollarSign, color: "emerald" }, { label: "Margem média", value: `${(reports.margin || 44.7).toFixed(1).replace(".", ",")}%`, detail: "Dentro da meta de 40%", icon: TrendingUp, color: "amber" }];
  const alerts = [{ type: "Crítico", title: `${summary.overdueOrders || 2} ordens estão atrasadas`, text: "A OP-0084 ultrapassou o prazo em 1 dia.", action: "Ver ordens", icon: Clock3, tone: "rose" }, { type: "Atenção", title: `${summary.budgetAlerts || 1} projeto pode ultrapassar o orçamento`, text: "O projeto PRJ-024 está 6,2% acima do previsto.", action: "Analisar custos", icon: CircleDollarSign, tone: "amber" }, { type: "Estoque", title: `${summary.stockAlerts || 3} materiais chegaram ao mínimo`, text: "Repor antes do início das próximas ordens.", action: "Repor itens", icon: PackageCheck, tone: "cyan" }, { type: "Cliente", title: `${summary.postSaleToday || 4} clientes entram no pós-venda hoje`, text: "A régua de relacionamento está pronta.", action: "Iniciar contato", icon: UsersRound, tone: "emerald" }];
  return <><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(metric => { const Icon = metric.icon; return <Card key={metric.label} className="group overflow-hidden rounded-2xl border-0 bg-white shadow-[0_8px_28px_rgba(25,45,90,0.05)]"><CardContent className="relative p-5"><div className={`absolute right-0 top-0 h-24 w-24 translate-x-8 -translate-y-8 rounded-full opacity-10 ${metric.color === "zinc" ? "bg-zinc-800" : metric.color === "violet" ? "bg-violet-500" : metric.color === "emerald" ? "bg-emerald-500" : "bg-amber-500"}`} /><div className="mb-5 flex items-start justify-between"><p className="text-xs font-medium text-[#8792a8]">{metric.label}</p><div className={`rounded-xl p-2 ${metric.color === "zinc" ? "bg-zinc-100 text-zinc-800" : metric.color === "violet" ? "bg-violet-50 text-violet-600" : metric.color === "emerald" ? "bg-emerald-50 text-emerald-600" : "bg-amber-50 text-amber-600"}`}><Icon className="h-4 w-4" /></div></div><p className="text-[28px] font-semibold tracking-[-0.05em] text-[#172033]">{metric.value}</p><p className="mt-2 text-[11px] font-medium text-emerald-600">{metric.detail}</p></CardContent></Card> })}</section><section className="mt-7 grid gap-6 xl:grid-cols-[1.35fr_1fr]"><Card className="rounded-2xl border-0 bg-white shadow-[0_8px_28px_rgba(25,45,90,0.05)]"><CardHeader className="flex flex-row items-center justify-between space-y-0 border-b border-[#eef1f6] px-6 py-5"><div><CardTitle className="text-base font-semibold">Central de atenção</CardTitle><p className="mt-1 text-xs text-[#96a0b2]">Prioridades que pedem uma ação hoje</p></div><Badge variant="outline" className="rounded-full border-rose-100 bg-rose-50 text-rose-600">6 alertas</Badge></CardHeader><CardContent className="p-0">{alerts.map((alert, index) => { const Icon = alert.icon; return <button onClick={() => setActive(alert.type === "Estoque" ? "stock" : alert.type === "Cliente" ? "post-sale" : alert.type === "Atenção" ? "costs" : "production")} key={alert.type} className="group flex w-full items-center gap-4 border-b border-[#f0f2f6] px-6 py-4 text-left last:border-0 hover:bg-[#fbfcfe]"><div className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${alert.tone === "rose" ? "bg-rose-50 text-rose-500" : alert.tone === "amber" ? "bg-amber-50 text-amber-500" : alert.tone === "cyan" ? "bg-cyan-50 text-cyan-600" : "bg-emerald-50 text-emerald-600"}`}><Icon className="h-[18px] w-[18px]" /></div><div className="min-w-0 flex-1"><div className="mb-1 flex items-center gap-2"><span className={`text-[10px] font-bold uppercase tracking-wider ${alert.tone === "rose" ? "text-rose-500" : alert.tone === "amber" ? "text-amber-500" : alert.tone === "cyan" ? "text-cyan-600" : "text-emerald-600"}`}>{alert.type}</span><span className="h-1 w-1 rounded-full bg-[#d7dce6]" /><p className="truncate text-sm font-medium text-[#273249]">{alert.title}</p></div><p className="truncate text-xs text-[#97a1b2]">{alert.text}</p></div><span className="hidden items-center gap-1 text-xs font-semibold text-zinc-800 opacity-0 transition group-hover:opacity-100 sm:flex">{alert.action}<ChevronRight className="h-3.5 w-3.5" /></span></button> })}</CardContent></Card><Card className="rounded-2xl border-0 bg-white shadow-[0_8px_28px_rgba(25,45,90,0.05)]"><CardHeader className="flex flex-row items-center justify-between space-y-0 px-6 pb-0 pt-5"><div><CardTitle className="text-base font-semibold">Ritmo de produção</CardTitle><p className="mt-1 text-xs text-[#96a0b2]">Ordens concluídas nesta semana</p></div><div className="rounded-lg bg-emerald-50 px-2 py-1 text-[11px] font-semibold text-emerald-600">+18,6%</div></CardHeader><CardContent className="px-4 pb-5 pt-6"><div className="h-[180px] w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={weekly} barSize={22}><CartesianGrid vertical={false} stroke="#eef1f6" /><XAxis dataKey="day" axisLine={false} tickLine={false} tick={{ fill: "#9aa3b5", fontSize: 11 }} /><YAxis hide /><Tooltip cursor={{ fill: "#f6f8fc" }} contentStyle={{ border: "0", borderRadius: "12px", boxShadow: "0 8px 24px rgba(25,45,90,.12)", fontSize: 12 }} /><Bar dataKey="value" radius={[6, 6, 2, 2]} fill="#27272a" /></BarChart></ResponsiveContainer></div></CardContent></Card></section><section className="mt-7 grid gap-6 xl:grid-cols-[1.35fr_1fr]"><Card className="rounded-2xl border-0 bg-white shadow-[0_8px_28px_rgba(25,45,90,0.05)]"><CardHeader className="flex flex-row items-center justify-between space-y-0 border-b border-[#eef1f6] px-6 py-5"><div><CardTitle className="text-base font-semibold">Projetos em andamento</CardTitle><p className="mt-1 text-xs text-[#96a0b2]">Acompanhe a saúde da sua carteira</p></div><Button variant="ghost" onClick={() => setActive("commercial")} className="gap-1 rounded-lg text-xs font-semibold text-zinc-800">Ver todos <ChevronRight className="h-3.5 w-3.5" /></Button></CardHeader><CardContent className="p-0">{projects.map(project => <div key={project.code} className="flex flex-col gap-3 border-b border-[#f0f2f6] px-6 py-4 last:border-0 sm:flex-row sm:items-center"><div className="flex min-w-0 flex-1 items-center gap-3"><div className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl ${project.tone === "blue" ? "bg-zinc-100 text-zinc-800" : project.tone === "violet" ? "bg-violet-50 text-violet-600" : "bg-emerald-50 text-emerald-600"}`}><BriefcaseBusiness className="h-4 w-4" /></div><div className="min-w-0"><div className="flex items-center gap-2"><span className="text-[10px] font-bold text-[#8a95aa]">{project.code}</span><span className="h-1 w-1 rounded-full bg-[#dce1eb]" /><p className="truncate text-sm font-semibold text-[#273249]">{project.name}</p></div><p className="mt-0.5 truncate text-xs text-[#9aa3b5]">{project.client}</p></div></div><div className="flex w-full items-center gap-4 sm:w-[240px]"><div className="flex-1"><div className="mb-1.5 flex justify-between text-[10px] font-medium"><span className="text-[#8290a5]">{project.status}</span><span className="text-[#273249]">{project.progress}%</span></div><Progress value={project.progress} className="h-1.5 bg-[#edf0f6]" /></div><div className="hidden text-right sm:block"><p className="text-xs font-semibold text-[#273249]">{project.value}</p><p className="mt-0.5 text-[10px] text-emerald-600">{project.margin} margem</p></div></div></div>)}</CardContent></Card><Card className="rounded-2xl border-0 bg-[#172033] text-white shadow-[0_8px_28px_rgba(25,45,90,0.14)]"><CardHeader className="px-6 pb-0 pt-5"><CardTitle className="text-base font-semibold">Distribuição da carteira</CardTitle><p className="mt-1 text-xs text-white/45">Projetos por etapa operacional</p></CardHeader><CardContent className="flex items-center gap-5 px-4 py-5"><div className="h-[180px] w-[180px] shrink-0"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={stageData} dataKey="value" nameKey="name" innerRadius={54} outerRadius={75} paddingAngle={4} stroke="none">{stageData.map(entry => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip contentStyle={{ border: "0", borderRadius: "12px", color: "#172033", fontSize: 12 }} /></PieChart></ResponsiveContainer></div><div className="min-w-0 flex-1 space-y-3">{stageData.map(item => <div key={item.name} className="flex items-center justify-between text-xs"><span className="flex items-center gap-2 text-white/60"><span className="h-2 w-2 rounded-full" style={{ background: item.color }} />{item.name}</span><strong className="font-semibold text-white">{item.value}%</strong></div>)}<div className="mt-4 border-t border-white/10 pt-3"><p className="text-[11px] leading-relaxed text-white/45">A carteira mantém um equilíbrio saudável entre produção e relacionamento.</p></div></div></CardContent></Card></section></>;
}

function ModulePlaceholder({ active, onBack }: { active: string; onBack: () => void }) {
  const item = modules.find(module => module.id === active) ?? modules[0]; const Icon = item.icon;
  const clientsQuery = trpc.clients.list.useQuery(undefined, { retry: false });
  const proposalsQuery = trpc.proposals.list.useQuery(undefined, { retry: false });
  const productionQuery = trpc.production.list.useQuery(undefined, { retry: false });
  const stockQuery = trpc.stock.list.useQuery(undefined, { retry: false });
  const suppliersQuery = trpc.suppliers.list.useQuery(undefined, { retry: false });
  const postSaleQuery = trpc.postSale.list.useQuery(undefined, { retry: false });
  const content: Record<string, { intro: string; stats: [string, string][]; rows: [string, string, string][] }> = {
    commercial: { intro: "Acompanhe propostas, clientes e conversões em um único pipeline.", stats: [["Propostas abertas", "8"], ["Em aprovação", "3"], ["Conversão média", "62%"]], rows: [["PRP-018", "Grupo Orion · Modernização", "Em aprovação"], ["PRP-017", "Casa Nômade · Identidade", "Enviada"], ["PRP-016", "Norte & Sul · Sinalização", "Aprovada"]] },
    production: { intro: "Ordens de produção, prazos e checklists sob controle da equipe.", stats: [["Ordens ativas", "16"], ["Em risco", "2"], ["Concluídas no mês", "28"]], rows: [["OP-0084", "Modernização da recepção", "Atrasada"], ["OP-0083", "Identidade visual completa", "Em andamento"], ["OP-0081", "Sinalização corporativa", "Concluída"]] },
    stock: { intro: "Visibilidade sobre materiais, reservas e requisições críticas.", stats: [["Itens cadastrados", "248"], ["Abaixo do mínimo", "3"], ["Requisições abertas", "7"]], rows: [["MAT-042", "Chapa MDF naval 15mm", "Repor"], ["MAT-018", "Adesivo vinílico branco", "Disponível"], ["MAT-007", "Perfil de alumínio 30mm", "Reservado"]] },
    suppliers: { intro: "Acompanhe fornecedores, condições e entregas que impactam seus prazos.", stats: [["Fornecedores ativos", "38"], ["Entregas pendentes", "4"], ["Em atraso", "1"]], rows: [["SUP-014", "Alumínios Prime", "Entrega hoje"], ["SUP-009", "Print House", "No prazo"], ["SUP-022", "MDF & Cia", "Em atraso"]] },
    costs: { intro: "Compare o previsto e o realizado para proteger a margem de cada projeto.", stats: [["Custo previsto", "R$ 10.200"], ["Custo realizado", "R$ 9.840"], ["Margem projetada", "44,7%"]], rows: [["PRJ-024", "Modernização da recepção", "+6,2% de desvio"], ["PRJ-021", "Identidade visual completa", "Dentro do plano"], ["PRJ-019", "Sinalização corporativa", "Margem protegida"]] },
    "post-sale": { intro: "Nunca perca o momento certo de cuidar do relacionamento e gerar recorrência.", stats: [["Contatos hoje", "4"], ["Satisfeitos", "18"], ["Oportunidades", "6"]], rows: [["Grupo Orion", "Pesquisa de satisfação", "Agendado hoje"], ["Casa Nômade", "Orientação de manutenção", "Pendente"], ["Norte & Sul", "Oportunidade de recompra", "Em negociação"]] },
    reports: { intro: "Relatórios gerenciais para transformar operação em decisão.", stats: [["Produtividade", "+18,6%"], ["Giro de estoque", "4,2x"], ["Projetos no prazo", "86%"]], rows: [["Produção", "Ordens concluídas por semana", "+18,6%"], ["Custos", "Realizado vs. previsto", "96,5%"], ["Pós-venda", "Contatos convertidos", "32%"]] },
  };
  const data = content[active] ?? content.commercial;
  const liveRows: [string, string, string][] = active === "commercial" && proposalsQuery.data?.length ? proposalsQuery.data.slice(0, 8).map(proposal => [proposal.code, proposal.title, proposal.status]) : active === "production" && productionQuery.data?.length ? productionQuery.data.slice(0, 8).map(order => [order.code, order.title, order.status]) : active === "stock" && stockQuery.data?.length ? stockQuery.data.slice(0, 8).map(item => [item.sku, item.name, Number(item.quantity) <= Number(item.minimumQuantity) ? "Repor" : "Disponível"]) : active === "suppliers" && suppliersQuery.data?.length ? suppliersQuery.data.slice(0, 8).map(supplier => [`SUP-${supplier.id}`, supplier.name, supplier.deliveryStatus]) : active === "post-sale" && postSaleQuery.data?.length ? postSaleQuery.data.slice(0, 8).map(entry => [`PS-${entry.id}`, `Projeto #${entry.projectId}`, entry.stage]) : data.rows;
  const exportCsv = () => { const csv = [["Código", "Registro", "Status"], ...liveRows].map(row => row.map(cell => `"${cell.replaceAll('"', '""')}"`).join(",")).join("\\n"); const blob = new Blob([csv], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = `${active}-relatorio.csv`; link.click(); URL.revokeObjectURL(url); };
  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-zinc-100 text-zinc-800"><Icon className="h-5 w-5" /></div><div><h2 className="text-2xl font-semibold tracking-tight">{item.label}</h2><p className="mt-1 text-sm text-[#8792a8]">{data.intro}</p></div></div></div><div className="flex gap-2"><Button variant="outline" onClick={onBack} className="rounded-xl">Voltar</Button><Button className="gap-2 rounded-xl bg-zinc-800 hover:bg-zinc-900"><Plus className="h-4 w-4" /> Novo registro</Button></div></div><div className="grid gap-4 sm:grid-cols-3">{data.stats.map(([label, value]) => <Card key={label} className="rounded-2xl border-0 bg-white shadow-soft"><CardContent className="p-5"><p className="text-xs text-[#8792a8]">{label}</p><p className="mt-3 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-[11px] font-medium text-emerald-600">Atualizado agora</p></CardContent></Card>)}</div><Card className="overflow-hidden rounded-2xl border-0 bg-white shadow-soft"><CardHeader className="flex flex-row items-center justify-between border-b border-[#eef1f6] px-6 py-5"><div><CardTitle className="text-base">Acompanhamento operacional</CardTitle><p className="mt-1 text-xs text-[#96a0b2]">Registros mais recentes do módulo</p></div><Button variant="outline" size="sm" onClick={exportCsv} className="hidden rounded-lg sm:flex">Exportar CSV</Button></CardHeader><CardContent className="p-0">{liveRows.map(([code, title, status]) => <div key={code} className="flex items-center gap-4 border-b border-[#f0f2f6] px-6 py-4 last:border-0"><div className="grid h-9 w-9 place-items-center rounded-xl bg-zinc-100 text-zinc-800"><FileText className="h-4 w-4" /></div><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-wider text-[#97a1b2]">{code}</p><p className="truncate text-sm font-semibold text-[#273249]">{title}</p></div><Badge variant="outline" className={`rounded-full border-0 text-[11px] ${status.includes("atras") || status.includes("desvio") || status.includes("Repor") ? "bg-rose-50 text-rose-600" : status.includes("hoje") || status.includes("Pendente") ? "bg-amber-50 text-amber-600" : "bg-emerald-50 text-emerald-600"}`}>{status}</Badge><ChevronRight className="h-4 w-4 text-[#c1c8d5]" /></div>)}</CardContent></Card></div>;
}
